# CI / CD

Status: pending

## Intended scope

Continuous integration and delivery: workflows under `.github/`, the
checks they run, and the relationship to publishing and deployment.

## TOC (stub)

- Workflow inventory (`.github/workflows/*`)
- Per-workflow trigger and jobs
- Caching strategy (pnpm, build, Playwright)
- Quality gates (typecheck, lint, test, e2e)
- Release / publish workflow (GitHub Packages)
- Web app deploy workflow
