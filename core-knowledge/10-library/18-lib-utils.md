# Library Utilities (`lib/`)

Status: pending

## Intended scope
Document shared utilities in `packages/ui/src/lib/`: pure helpers used
across components and hooks. For copy-paste distribution every util must
be self-contained or its dependencies must be stated explicitly.

## TOC (stub)
- Utility inventory
- Per-utility contract (pure? referential transparency? side effects?)
- Cross-component usage map
- Copy-paste portability rules
