# Hooks

Status: pending

## Intended scope
Document every hook in `packages/ui/src/hooks/`: signature, purpose,
side effects, dependencies, expected lifecycle, and shadcn-style
copy-paste considerations (each hook should be liftable on its own).

## TOC (stub)
- Hook inventory
- Per-hook contract (inputs, outputs, side effects)
- Composition patterns
- Testing conventions for hooks
- Copy-paste portability checklist
