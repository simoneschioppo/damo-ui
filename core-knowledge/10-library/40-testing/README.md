# Testing (Library)

Status: pending

## Intended scope
Testing strategy for `packages/ui`: Vitest config (`vitest.config.ts`,
`vitest.setup.ts`), test conventions, the mocks scaffolding in
`packages/ui/src/mocks/`, coverage targets, and how tests run in CI.

End-to-end tests live at the repo root (`/e2e`) — see
`30-cross-cutting/15-e2e.md`.

## TOC (stub)
- Vitest setup and config
- Test file colocation conventions
- Mocks scaffolding (`src/mocks/`)
- Coverage policy and thresholds
- Component test patterns (RTL, accessibility queries)
- Running tests locally and in CI
